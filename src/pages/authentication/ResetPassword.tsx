import {AuthSimpleLayout, ResetPasswordForm} from "@frs535/react-ui-components";

const ResetPassword = () => {
  return (
    <AuthSimpleLayout>
      <ResetPasswordForm />
    </AuthSimpleLayout>
  );
};

export default ResetPassword;
